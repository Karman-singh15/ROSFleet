# Where the FastAPI backend is reachable from the BROWSER.
# Must be a URL the browser can reach, not a Docker-internal hostname.
NEXT_PUBLIC_API_URL=http://localhost:8000
NEXT_PUBLIC_WS_URL=ws://localhost:8000/ws
