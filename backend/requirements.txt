# Web framework
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
pydantic-settings==2.7.0

# Database
sqlalchemy==2.0.36
alembic==1.14.0
psycopg2-binary==2.9.10      # PostgreSQL driver (production)
# SQLite needs no driver - it is built into Python, and is what the tests use

# ROS connection
roslibpy==2.1.0              # rosbridge websocket client

# Utilities
python-multipart==0.0.20     # map file uploads
httpx==0.28.1

# Testing
pytest==8.3.4
pytest-asyncio==0.25.0
